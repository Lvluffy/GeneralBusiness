<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="45dp"
    android:layout_margin="15dp"
    android:background="@drawable/login_editext_selector"
    android:paddingLeft="15dp"
    android:paddingRight="15dp">

    <TextView
        android:id="@+id/txt_verification_code_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:gravity="center|left"
        android:text="验证码"
        android:textColor="#333333"
        android:textSize="13sp" />

    <EditText
        android:id="@+id/edit_verification_code"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/txt_verification_code_hint"
        android:layout_toRightOf="@+id/txt_verification_code_hint"
        android:background="@null"
        android:hint="请输入验证码"
        android:inputType="number"
        android:maxLength="6"
        android:paddingLeft="15dp"
        android:paddingStart="15dp"
        android:textColor="#333333"
        android:textColorHint="#CCCCCC"
        android:textCursorDrawable="@drawable/base_cursor_color"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/txt_get_verification_code"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:gravity="center"
        android:text="获取验证码"
        android:textColor="@color/colorPrimary"
        android:textSize="13sp" />

</RelativeLayout>

